@model IEnumerable<apteka.Models.Akqia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naz)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pr)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naz)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pr)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDAk">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDAk">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDAk">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
